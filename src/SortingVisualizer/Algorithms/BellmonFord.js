// Implement Bellmon Ford Algorithm for finding shortest path in a graph 

var running = true;

const elementSelect = (id) => {
    return document.getElementById(id);
}

const delay = (ms) => {
    return new Promise((resolve) => setTimeout(resolve, ms));
}

